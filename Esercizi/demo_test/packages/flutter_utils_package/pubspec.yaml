name: flutter_utils_package
description: Pacchetto riusabile con utilities comuni per applicazioni Flutter
version: 1.0.0
homepage: https://github.com/example/flutter_utils_package
repository: https://github.com/example/flutter_utils_package

environment:
  sdk: ^3.11.0
  flutter: '>=3.41.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# Nessuna dipendenza da piattaforme specifiche
# Questo è un pure Dart package
flutter:
  # No assets needed for this package
